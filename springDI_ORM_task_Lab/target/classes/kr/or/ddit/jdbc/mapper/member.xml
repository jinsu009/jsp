<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.or.ddit.jdbc.IMemberDAO">
	<select id="selectMember" resultType="kr.or.ddit.vo.MemberVO" parameterType="string" >
		SELECT * FROM MEMBER
		WHERE MEM_ID = #{mem_id}
	</select>
</mapper>